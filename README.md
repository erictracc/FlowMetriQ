your-app/
│── app.py
│── config/
│     └── settings.json        ← secret place with IP, port, DB settings
│── config_manager.py          ← loads and saves settings
│── pages/
│     ├── home.py
│     └── config_page.py       ← optional UI to change settings
│── requirements.txt
│── README.md
